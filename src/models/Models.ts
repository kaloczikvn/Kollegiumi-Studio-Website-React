export interface ModelEvent {
    title: string;
    subTitle: string;
    image?: string;
    url?: string;
}

export interface ModelTeam {
    name: string;
    role?: string;
    image?: string;
}
